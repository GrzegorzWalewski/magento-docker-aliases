alias bm="docker exec -it -u owner (basename (pwd -P))_php_1 bin/magento"
alias cf="docker exec -it -u owner (basename (pwd -P))_php_1 bin/magento c:fl"
alias cc="docker exec -it -u owner (basename (pwd -P))_php_1 bin/magento c:c"
alias rg="docker exec -it -u owner (basename (pwd -P))_php_1 rm -rf generated/"
alias re="docker exec -it -u owner (basename (pwd -P))_php_1 bin/magento i:rei"
alias up="docker exec -it -u owner (basename (pwd -P))_php_1 bin/magento s:up"
alias dc="docker exec -it -u owner (basename (pwd -P))_php_1 bin/magento s:di:compile"
alias ci="docker exec -it -u owner (basename (pwd -P))_php_1 composer2 install"
alias dbash="docker exec -it -u owner (basename (pwd -P))_php_1 bash"
alias dmysql="docker exec -it (basename (pwd -P))_mysql_1 mysql -uroot"
